import {createSlice} from '@reduxjs/toolkit';

const subStoreSlice = createSlice({
    name: 'placeholder',
    initialState: {
        placeholder: {}
    },
    reducers: {}
})

export default subStoreSlice.reducer;